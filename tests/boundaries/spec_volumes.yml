
      version: "2"
      services:
        service:
          volumes:
            - service_log:/log
      volumes:
        service_log: {}

---------------------------------
options: --boundaries
expected: |
      skinparam componentStyle uml2
      cloud system {
        [service]
      }
      database service_log {
        [/log] as volume_1
      }
      [service] --> volume_1

